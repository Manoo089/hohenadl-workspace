{
  "name": "manuel-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:test": "turbo dev --filter=test",
    "dev:portfolio": "turbo dev --filter=portfolio",
    "preview:emails": "pnpm --filter @repo/transactional dev --port 3001",
    "build": "turbo build",
    "build:portfolio": "turbo build --filter=portfolio",
    "start": "turbo start",
    "start:portfolio": "turbo start --filter=portfolio",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.33.0",
  "dependencies": {
    "zod": "^4.4.1"
  }
}
